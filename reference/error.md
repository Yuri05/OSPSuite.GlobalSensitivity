# error

Stops if `condition` is `TRUE`

## Usage

``` r
error(condition, errorMessage = NULL)
```

## Arguments

- condition:

  A logical condition

- errorMessage:

  Message to be printed before stopping in case `condition` is `TRUE`
