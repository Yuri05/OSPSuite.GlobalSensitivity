# buildSettingsCMD

Build a summary of all parameter and outputs selected for a sensitivity
analysis.

## Usage

``` r
buildSettingsCMD(parameters, outputs)
```

## Arguments

- parameters:

  A list of `SAParameter` objects.

- outputs:

  A list of `SAOutput` objects.

## Value

A summary of all parameter and outputs selected for a sensitivity
analysis
