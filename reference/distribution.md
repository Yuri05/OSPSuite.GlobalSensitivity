# distribution

A named list of distribution constructors used to create distribution
objects for global sensitivity analyses. Each element is a constructor
function returning a distribution object: `Uniform`, `LogUniform`,
`Normal` and `LogNormal`.

## Usage

``` r
distribution
```

## Format

A named `list` of distribution constructor functions.
